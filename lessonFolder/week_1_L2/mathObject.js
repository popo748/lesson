//constant with uppercase letter
let radius = 3;
let areaOfCircle = Math.PI * radius * radius;
console.log(areaOfCircle.toFixed(2));

let legA = 3;
let legB = 4;
let hypotenuse = Math.sqrt(legA * legA + legB * legB);
console.log(hypotenuse);
